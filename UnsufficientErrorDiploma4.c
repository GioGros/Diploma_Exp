#include <stdio.h>
#include <stdlib.h>

int main() {
    FILE *file = fopen("nonexistent_file.txt", "r");
    if (file == NULL) {
        // Неправильное обращение с ошибками: не обработка ошибки открытия файла
        printf("Ошибка открытия файла\n");
        return 1; // Программа завершается без дальнейшей обработки ошибки
    }

    // Неправильное обращение с ошибками: непроверенное выделение памяти
    int *ptr = (int *)malloc(-1 * sizeof(int)); // Попытка выделить отрицательное количество памяти
    if (ptr == NULL) {
        printf("Ошибка выделения памяти\n");
        fclose(file); // Не освобождаем ресурсы, так как ошибка выделения памяти
        return 1; // Программа завершается без дальнейшей обработки ошибки
    }

    // Неправильное обращение с ошибками: игнорирование возвращаемого значения функции
    int result = fclose(file); // Не проверяем успешное закрытие файла
    if (result != 0) {
        printf("Ошибка закрытия файла\n");
        // Программа продолжает работу, игнорируя ошибку закрытия файла
    }

    return 0;
}